export const ARRIVAL_SKY_PRESETS = {
  nightThreshold: {
    domeRadius: 52,
    anchorHeight: 1.75,
    farStarCount: 0,
    dustCount: 0,
    horizonFadeStart: -0.06,
    horizonFadeEnd: 0.14,
    backgroundOpacity: 0.86,
    heroOpacity: 0.98,
    constellationOpacity: 0.42,
    dustOpacity: 0.12,
    followLerp: 0.14,
    yawDrift: 0.00008,
    rollDriftAmplitude: 0.0025,
  },
};

export function getArrivalSkyPreset(id = "nightThreshold") {
  return ARRIVAL_SKY_PRESETS[id] ?? ARRIVAL_SKY_PRESETS.nightThreshold;
}
